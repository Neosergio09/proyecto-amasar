---
import MainLayout from '../layouts/MainLayout.astro';
import { supabase } from '../lib/supabase';
import { Image } from 'astro:assets';
import heroImage from '../assets/unnamed.jpg';

// SEO y Copywriting Estratégico: Enfoque Multicanal (B2B + B2C)
const title = "Amasar | Galletas Artesanales, Café y Detalles para Momentos Únicos";
const description = "El sabor artesanal de Bogotá en tu mesa. Galletas decoradas, café premium y jugos naturales para panaderías, eventos corporativos y celebraciones especiales.";

// Obtenemos productos destacados para mostrar la variedad
const { data: destacados } = await supabase
  .from('productos')
  .select('*')
  .limit(3);
---

<MainLayout title={title} description={description}>
  <section class="relative min-h-[90vh] flex items-center pt-20 overflow-hidden bg-background">
    <div class="max-w-7xl mx-auto px-6 grid md:grid-cols-2 gap-16 items-center">
      <div class="z-10">
        <div class="inline-block py-1 px-4 rounded-full bg-primary/10 mb-6">
          <span class="text-primary font-bold text-xs uppercase">...</span>
          <span class="text-primary brightness-75 font-black text-xs uppercase tracking-widest">
            ARTESANÍA LOCAL • BOGOTÁ, CO
          </span>
        </div>

        <h1 class="text-6xl md:text-7xl font-serif leading-[1.1] mb-8 text-secondary">
          El sabor que <span class="italic text-primary">transforma</span> cada reunión.
        </h1>
        <p class="text-xl text-slate-600 mb-10 max-w-lg leading-relaxed">
          Desde el detalle perfecto para un **cumpleaños**, el refrigerio de tu **empresa**, hasta el stock de tu **panadería**. Galletas, café y jugos con entrega garantizada.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="/productos" class="bg-secondary text-white px-10 py-5 rounded-2xl font-bold text-lg hover:bg-primary transition-all shadow-xl shadow-secondary/10">
            Explorar Catálogo
          </a>
          <a href="/contacto" class="bg-white border-2 border-slate-200 text-secondary px-10 py-5 rounded-2xl font-bold text-lg hover:border-primary transition-all">
            Cotizar Evento
          </a>
        </div>
        <div class="mt-12 pt-8 border-t border-slate-200 flex gap-8 opacity-60">
            <p class="text-[10px] font-black uppercase tracking-tighter">✨ Panaderías</p>
            <p class="text-[10px] font-black uppercase tracking-tighter">💼 Empresas</p>
            <p class="text-[10px] font-black uppercase tracking-tighter">🎂 Fiestas</p>
        </div>
      </div>

      <div class="relative">
        <div class="aspect-[4/5] rounded-[3rem] overflow-hidden shadow-2xl rotate-2 hover:rotate-0 transition-transform duration-700">
          <Image 
            src={heroImage} 
            alt="Galletas artesanales decoradas para regalos y eventos en Bogotá" 
            class="w-full h-full object-cover scale-110 hover:scale-100 transition-transform duration-1000"
            loading="eager"
            format="webp"
          />
        </div>
        <div class="absolute -bottom-10 -left-10 bg-primary p-10 rounded-full text-white shadow-2xl border-8 border-background animate-bounce-slow">
            <p class="text-center font-bold">
                <span class="block text-4xl">Bogotá</span>
                <span class="text-[10px] uppercase tracking-tighter">Envío Express</span>
            </p>
        </div>
      </div>
    </div>
  </section>

  <section class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-6">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="p-12 rounded-[3rem] bg-slate-50 border border-slate-100 group hover:bg-primary transition-all duration-500">
                <p class="text-4xl mb-6">🏪</p>
                <h3 class="text-2xl font-serif mb-4 group-hover:text-white">Para Negocios</h3>
                <p class="text-slate-500 group-hover:text-white/80">Suministro recurrente para panaderías y cafeterías que buscan calidad premium constante.</p>
            </div>
            <div class="p-12 rounded-[3rem] bg-slate-50 border border-slate-100 group hover:bg-secondary transition-all duration-500">
                <p class="text-4xl mb-6">🏢</p>
                <h3 class="text-2xl font-serif mb-4 group-hover:text-white">Corporativo</h3>
                <p class="text-slate-500 group-hover:text-white/80">Refrigerios para reuniones, regalos empresariales y lanzamientos de marca en Bogotá.</p>
            </div>
            <div class="p-12 rounded-[3rem] bg-slate-50 border border-slate-100 group hover:bg-primary transition-all duration-500">
                <p class="text-4xl mb-6">🎁</p>
                <h3 class="text-2xl font-serif mb-4 group-hover:text-white">Celebraciones</h3>
                <p class="text-slate-500 group-hover:text-white/80">Fiestas de cumpleaños, reuniones familiares y detalles personalizados para tu hogar.</p>
            </div>
        </div>
    </div>
  </section>

  <section class="py-32 bg-background">
    <div class="max-w-7xl mx-auto px-6">
      <div class="text-center mb-20">
        <h2 class="text-4xl md:text-5xl font-serif text-secondary mb-4">Todo lo que necesitas para <span class="text-primary italic">sorprender</span></h2>
        <div class="h-1 w-20 bg-primary mx-auto"></div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        {[
          { name: 'Galletas', desc: 'Decoradas y personalizadas', icon: '🍪' },
          { name: 'Chocolatería', desc: 'Detalles que enamoran', icon: '🍫' },
          { name: 'Café', desc: 'De origen para tu oficina', icon: '☕' },
          { name: 'Jugos', desc: '100% naturales y frescos', icon: '🥤' }
        ].map((cat) => (
          <div class="group p-10 rounded-[2.5rem] bg-white border border-slate-100 hover:border-primary transition-all hover:shadow-2xl text-center">
            <span class="text-5xl mb-6 block transform group-hover:scale-110 transition-transform">{cat.icon}</span>
            <h3 class="text-xl font-serif mb-2 group-hover:text-primary transition-colors">{cat.name}</h3>
            <p class="text-slate-400 text-xs uppercase tracking-widest font-bold">{cat.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  {destacados && destacados.length > 0 && (
    <section class="py-32 bg-white">
      <div class="max-w-7xl mx-auto px-6">
        <div class="flex justify-between items-end mb-16">
            <h2 class="text-4xl font-serif text-secondary leading-tight">Nuestros favoritos <br/><span class="text-primary italic">del mes</span></h2>
            <a href="/productos" class="text-primary font-bold border-b-2 border-primary pb-1 uppercase text-xs tracking-widest">Ver catálogo completo</a>
        </div>
        <div class="grid md:grid-cols-3 gap-12">
          {destacados.map((prod) => (
            <div class="group cursor-pointer">
              <div class="aspect-square bg-slate-50 rounded-[3rem] mb-6 overflow-hidden border border-slate-100">
                <img 
                    src={prod.imagen_url || '/placeholder.jpg'} 
                    alt={prod.nombre} 
                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" 
                />
              </div>
              <h4 class="font-serif text-2xl text-secondary mb-2">{prod.nombre}</h4>
              <p class="text-slate-500 text-sm mb-4 line-clamp-2">{prod.descripcion}</p>
              <p class="text-primary font-black text-sm uppercase tracking-widest">Consultar disponibilidad</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}
</MainLayout>

<style>
  @keyframes bounce-slow {
    0%, 100% { transform: translateY(0) rotate(2deg); }
    50% { transform: translateY(-20px) rotate(-2deg); }
  }
  .animate-bounce-slow {
    animation: bounce-slow 6s infinite ease-in-out;
  }
</style>